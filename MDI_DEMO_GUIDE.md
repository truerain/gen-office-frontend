# Gen-Office MDI Demo 실행 가이드

## 📦 설치 및 실행

### 1. 의존성 설치
```bash
pnpm install
```

### 2. MDI 패키지 빌드
```bash
pnpm build --filter=@gen-office/mdi
```

### 3. Demo 앱 실행
```bash
pnpm demo
```

브라우저에서 http://localhost:5173 으로 접속하면 Demo 앱이 실행됩니다.

## 🎯 MDI Demo 페이지 기능

Demo 앱 상단 네비게이션에서 **"MDI"** 메뉴를 클릭하면 MDI 데모 페이지로 이동합니다.

### 주요 기능

#### 1. 탭 위치 선택
- **상단 (Top)**: 탭 바가 콘텐츠 영역 위에 표시
- **하단 (Bottom)**: 탭 바가 콘텐츠 영역 아래에 표시

#### 2. 최대 탭 개수 제한
- 원하는 최대 탭 개수를 설정 (예: 5, 10)
- 설정한 개수를 초과하면 새 탭 추가가 거부됨
- 경고 메시지 표시

#### 3. 샘플 탭 종류

**고객 상세 (Customer Detail)**
- 고객 정보 입력 폼
- 메모 작성 기능
- 상태 유지 테스트: 입력값이 탭 전환 후에도 유지됨

**주문 목록 (Order List)**
- 검색 기능
- 데이터 테이블
- 상태 유지 테스트: 검색어가 유지됨

**재고 관리 (Inventory Management)**
- 카테고리 선택
- 통계 카드 표시
- 상태 유지 테스트: 선택한 카테고리가 유지됨

**리포트 (Reports)**
- 여러 리포트 탭을 동시에 열어서 비교 가능
- 차트 영역 표시

**설정 (Settings)**
- 체크박스 상태
- 상태 유지 테스트: 체크 상태가 유지됨

### 4. 상태 유지 확인하기

1. 여러 탭 열기 (예: 고객 001, 고객 002, 주문 목록)
2. 각 탭에서 데이터 입력
   - 고객 001: 메모 작성
   - 고객 002: 고객명 입력
   - 주문 목록: 검색어 입력
3. 탭을 전환하며 이동
4. **✅ 모든 입력값이 그대로 유지됨을 확인!**

## 🔧 코드에서 MDI 사용하기

### 기본 사용법

```tsx
import { MDIContainer, useMDIStore } from '@gen-office/mdi';
import '@gen-office/mdi/index.css';

function MyApp() {
  const addTab = useMDIStore(state => state.addTab);

  const openNewTab = () => {
    addTab({
      id: 'unique-id',
      title: '탭 제목',
      content: <YourComponent />,
      icon: <Icon />,
      closable: true
    });
  };

  return (
    <div style={{ height: '100vh' }}>
      <button onClick={openNewTab}>탭 열기</button>
      <MDIContainer maxTabs={10} tabPosition="top" />
    </div>
  );
}
```

### Store 액션 사용

```tsx
const {
  tabs,           // 모든 탭 목록
  activeTabId,    // 현재 활성 탭 ID
  addTab,         // 탭 추가
  removeTab,      // 탭 제거
  setActiveTab,   // 활성 탭 설정
  updateTab,      // 탭 정보 업데이트
  closeAllTabs,   // 모든 탭 닫기
  closeOtherTabs  // 다른 탭 모두 닫기
} = useMDIStore();
```

## 📁 프로젝트 구조

```
gen-office-frontend/
├── packages/
│   ├── mdi/                    # MDI 패키지
│   │   ├── src/
│   │   │   ├── components/     # MDI 컴포넌트들
│   │   │   ├── store/          # Zustand 스토어
│   │   │   └── types/          # TypeScript 타입
│   │   └── package.json
│   ├── primitives/             # 기본 UI 컴포넌트
│   ├── theme/                  # 테마 시스템
│   └── ...
└── apps/
    └── demo/                   # Demo 앱
        └── src/
            └── pages/
                └── MDIPage.tsx # MDI 데모 페이지
```

## 🎨 스타일 커스터마이징

MDI 컴포넌트는 CSS 변수를 사용하므로 쉽게 커스터마이징할 수 있습니다:

```css
:root {
  --color-primary: #your-color;
  --color-background: #your-background;
  --border-radius-md: 8px;
  /* ... */
}
```

## 🐛 문제 해결

### 빌드 오류가 발생하는 경우
```bash
# 모든 캐시 삭제 후 재설치
pnpm clean
pnpm install
pnpm build
```

### 탭 스타일이 제대로 나오지 않는 경우
CSS 파일이 import되었는지 확인:
```tsx
import '@gen-office/mdi/index.css';
```

## 📚 더 알아보기

- [MDI 패키지 README](./packages/mdi/README.md)
- [Primitives 컴포넌트](./packages/primitives/README.md)
- [Theme 시스템](./packages/theme/README.md)

## 🤝 기여하기

이슈나 개선사항이 있으시면 GitHub Issues를 통해 알려주세요!
